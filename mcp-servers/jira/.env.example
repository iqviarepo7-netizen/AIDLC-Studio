# Optional fallback if Studio Connect fields are left empty.
# Prefer entering Base URL, email, and token in the AIDLC Studio form.
JIRA_BASE_URL=
JIRA_EMAIL=
JIRA_API_TOKEN=

# Optional: force API version / auth (otherwise auto-detected)
# JIRA_API_VERSION=3
# JIRA_AUTH_SCHEME=basic
